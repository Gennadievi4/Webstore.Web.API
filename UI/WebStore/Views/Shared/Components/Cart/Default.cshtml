@model CartViewModel

@{ 
    var items_count = Model.ItemsCount;
    var count = items_count > 0 ? $"({items_count})" : null;
}

<a asp-action="CartIndex" asp-controller="Cart"><i class="fa fa-shopping-cart"></i> Корзина @count</a>